// font-weight
enum FONT_WEIGHT { regular, medium, bold, black }

// pop-up message type
enum POP_UP_MESSAGE_TYPE { success, error, warning, confirm, none }

// text field border
enum BORDER_TYPE { none, outline, underline }

// order status
enum ORDER_STATUS { ordered, delivered, returned }

// bloc status
enum BLOC_STATE_STATUS { initial, loading, success, failure }
